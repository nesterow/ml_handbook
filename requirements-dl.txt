# ML Practical Handbook — Deep Learning (Phase 2: notebooks 16, 17, 18)
# Python 3.12. Install AFTER requirements.txt:
#   .venv/bin/pip install -r requirements-dl.txt
#
# These versions are what the DL notebooks were built and executed against.
# See scripts/bootstrap.sh for a one-shot setup + GPU audit.

# --- Deep learning framework ---
# discrete AMD card use AMD's ROCm-pinned wheel (see scripts/bootstrap.sh).
tensorflow-cpu==2.21.0

# --- Dataset access ---
# keras.datasets covers Fashion-MNIST, CIFAR-10, IMDB (used directly by the
# notebooks). tensorflow-datasets is included for the few cases where we want a
# clean tf.data pipeline or a dataset not in keras.datasets.
tensorflow-datasets==4.9.10

# NOTE: keras is pulled in automatically as a transitive dependency of
# tensorflow (keras==3.15.0), so it is NOT listed separately here. If you ever
# want the standalone Keras 3 with a non-TF backend (PyTorch/JAX), install
# `keras` on its own and set os.environ["KERAS_BACKEND"] before importing.
